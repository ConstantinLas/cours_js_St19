'use strict';

for (let index = 0; index < 10; index++) {
    if (index === 3) {
        //break;
        continue;
    }
    console.log(index);
    
}

for (let index_ = 0; index_ < 10; index_++) {
    if (index_ === 3) {
        break;
    }
    console.log(index_);
    
}